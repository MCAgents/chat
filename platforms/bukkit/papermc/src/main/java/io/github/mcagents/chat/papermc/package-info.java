/**
 * PaperMC platform entry point.
 *
 * <p>Holds only what PaperMC needs on top of the shared Bukkit code in
 * {@code io.github.mcagents.chat.bukkit}. Paper is a superset of the Spigot
 * API, so a Paper only type used here does not run on SpigotMC — and that
 * fails at runtime, not at compile time.</p>
 */
package io.github.mcagents.chat.papermc;
