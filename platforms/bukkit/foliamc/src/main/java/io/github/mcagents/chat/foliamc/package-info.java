/**
 * Folia platform entry point.
 *
 * <p>Holds only what Folia needs on top of the shared Bukkit code in
 * {@code io.github.mcagents.chat.bukkit}, which in practice means the
 * regionised schedulers that replace Folia's absent main thread.</p>
 */
package io.github.mcagents.chat.foliamc;
